/**
 * White-label branding. Change these to show the same dashboard to a different client.
 * Nothing else in the UI hard-codes a company name.
 */
export const BRAND = {
  name: 'Bank',                       // short mark in the sidebar
  sub: 'Dashboard',                   // line under the mark
  company: 'Lending Operations',      // eyebrow on login / setup pages
  product: 'Leadership Dashboard',    // page title, report footer
  emailDomain: 'bank.in',             // demo e-mail addresses
};
